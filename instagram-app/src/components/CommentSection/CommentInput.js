import React from 'react';

const CommentInput = props => {
    return (
        <form>
            <input typre="text" placeholder="Add comment..." />
        </form>
    )
}

export default CommentInput;